package com.buct.adminbackend.repository;

import com.buct.adminbackend.entity.ReviewStrategyConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReviewStrategyConfigRepository extends JpaRepository<ReviewStrategyConfig, Long> {
}
